sudo: required

services:
    - docker

before_install:
    - docker build -t awsomens/docker-learn1 -f Dockerfile.dev .

Script:
    - docker run -e CI=true awesomens/docker-learn1 npm run test 

language: generic

